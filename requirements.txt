# mr-whisper — dependências (cross-platform)
PySide6>=6.6
sounddevice>=0.4
pynput>=1.7
pyperclip>=1.8
requests>=2.28

# Linux também usa evdev pro hold-to-talk (no-op no Mac/Win via marker de ambiente)
evdev>=1.6 ; sys_platform == "linux"
